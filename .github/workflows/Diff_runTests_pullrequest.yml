name: PullRequest - Diff to Ancestor, Publish and attach Report

on:
  pull_request:
    branches: [ main ]

jobs:
  my-job:
    name: Diff to Ancestor, Publish and attach Report
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      - name: Set up MATLAB
        uses: matlab-actions/setup-matlab@v3
        with:
          release: R2023a
          products: Simulink Simulink_Test   # ensures comparison toolbox is present

      - name: Compare Models to Ancestors
        uses: matlab-actions/run-command@v2.1.1
        with:
          command: branch = "${{ github.head_ref }}"; diffGitHub_pullrequest(branch);

      - name: Debug — list HTML files
        if: always()
        run: |
          echo "=== HTML files in workspace ==="
          find ${{ github.workspace }} -name "*.html" 2>/dev/null \
            || echo "NO HTML FILES FOUND"

      - name: Upload Comparison Reports
        if: always()
        uses: actions/upload-artifact@v4.6.2
        with:
          name: diffreports
          path: ${{ github.workspace }}/*.html
          if-no-files-found: warn
